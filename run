#!/usr/bin/env nu
 
def "main web" [] {
  let server_port = (port 8080);
  cd ours
  cargo run -- ~/ $server_port
}

def "main desk" [] {
  cd desktop/
  cargo run
}

def "main build" [] {
  cd desktop/
  cargo build --release
}

def "main install" [] {
  main build 
  cargo install --path ./desktop
}

def main [] {}
